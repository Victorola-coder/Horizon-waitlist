export default function header() {
  return (
    <div>
      <header className={``}>
        <nav className={`flex items-center justify-between`}>
          <h2 >Horizon</h2>
        </nav>
      </header>
    </div>
  );
}
